// So, this library is kind of weird. Basically the header and the cpp
// file are all packaged into one. Including this essentially turns this
// into the cpp file which can be compiled.

#define STB_IMAGE_IMPLEMENTATION
#include "stb_image.h"
